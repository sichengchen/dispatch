{
  "name": "@dispatch/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "generate": "drizzle-kit generate",
    "migrate": "pnpm rebuild better-sqlite3 && drizzle-kit migrate",
    "seed": "node --import tsx/esm src/seed.ts",
    "verify": "node --import tsx/esm src/verify.ts",
    "test": "pnpm run build"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^25.2.0",
    "drizzle-kit": "^0.31.8",
    "tsx": "^4.21.0"
  }
}
