{
  "name": "@dispatch/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm --filter @dispatch/db build && pnpm --filter @dispatch/lib build && pnpm --filter @dispatch/server build && tsc -b",
    "test": "pnpm run build"
  },
  "dependencies": {
    "@dispatch/lib": "workspace:*",
    "@dispatch/server": "workspace:*"
  }
}
